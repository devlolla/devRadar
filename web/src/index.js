import React from 'react';
import ReactDOM from 'react-dom';

import App from './App';


ReactDOM.render(<App />, document.getElementById('root')); //App é a pasta App.js que guarda o HTML dentro do JavaScript, 
                                                          //ela vai ser renderizada(app) dentro do Root que é um ID na pasta index.html

 
// desafio: criar um botão p/ excluir o Dev
// criar um form de edição
                                                          